{
  "schema": "autobuilder.intent_card.v1",
  "prd_source": "/home/jsy/Documents/PRDs/build-queue/PRD-adopt-exit-code-and-docket-bridge.md",
  "intent_slug": "adopt-exit-code-and-docket-bridge",
  "root_motivation": "The self-review skill's `adopt_scan_probe` playbook branches on `adopt scan`'s exit code: 0 means \"all artifacts current\", 1 means \"at least one not-installed or installed-stale\". The Scan arm in `src/cli.rs:160-165` prints results and returns `Ok(())` unconditionally, unlike the Apply (`cli.rs:174-182`) and Verify (`cli.rs:207-213`) arms, which `bail!()` on actionable items. The 2026-09-04 self-review observed exit 0 alongside 22 `not-installed` verdicts; the 2026-08-31 review logged the same anomaly.",
  "user_persona": "Joe Yen on the wintermute laptop, running daily self-review and /build automation. Needs a machine-readable verdict + copy-pasteable fix per artifact so downstream tools (adopt-apply, docket-report) can consume it.",
  "unfakeable_metric": {
    "name": "acceptance_tests_passing_count",
    "lower_is_better": false,
    "harness_command": "cargo test --workspace --no-fail-fast 2>&1 | grep -c 'test .* ok'",
    "target": 10
  },
  "acceptance_criteria": [
    {
      "id": "AC1",
      "level": "MUST",
      "description": "scan_exit_actionable: Given a lineage with at least one `not-installed` artifact, When `adopt scan --format json` runs, Then stdout is valid JSON listing that verdict And the process exits 1.",
      "test": "tests/acceptance_ac1.rs"
    },
    {
      "id": "AC2",
      "level": "MUST",
      "description": "scan_exit_clean: Given a lineage where every artifact is `installed-current` or `not-a-bin`, When `adopt scan` runs, Then the process exits 0.",
      "test": "tests/acceptance_ac2.rs"
    },
    {
      "id": "AC3",
      "level": "MUST",
      "description": "scan_exit_stale: Given a lineage with at least one `installed-stale` artifact and zero `not-installed`, When `adopt scan` runs, Then the process exits 1.",
      "test": "tests/acceptance_ac3.rs"
    },
    {
      "id": "AC4",
      "level": "SHOULD",
      "description": "scan_exit_match_filter: Given one `not-installed` artifact named `foo` and the rest current, When `adopt scan --match 'bar*'` runs, Then the process exits 0.",
      "test": "tests/acceptance_ac4.rs"
    },
    {
      "id": "AC5",
      "level": "MUST",
      "description": "docket_bridge_resolve_argv: Given a stub `docket` binary on PATH that records its argv, When `adopt report` resolves a finding with slug `wm-node`, Then the stub receives exactly `resolve wm-node` (plus `--reason <text>` if and only if a reason is passed) and no `--run` or `--key` token.",
      "test": "tests/acceptance_ac5.rs"
    },
    {
      "id": "AC6",
      "level": "SHOULD",
      "description": "docket_bridge_error_log: Given a stub `docket` that exits 2 on any argv, When `adopt report` runs, Then stderr contains the full argv adopt attempted before the exit-status line.",
      "test": "tests/acceptance_ac6.rs"
    },
    {
      "id": "AC7",
      "level": "MAY",
      "description": "docket_bridge_live: Given the real `docket` binary installed at ~/.local/bin/docket, When `adopt report --run <fresh-run-id>` runs on this box, Then it exits 0 with no clap usage error in output.",
      "test": "tests/acceptance_ac7.rs"
    }
  ],
  "scope": [
    "Walk ~/wintermute/*/ for Cargo.toml declaring [[bin]]",
    "Additive scan of shipped slugs from build manifest",
    "Per-artifact verdict: not-installed, installed-stale, installed-current, not-a-bin",
    "Produce copy-pasteable fix_cmd for each actionable verdict",
    "JSON and table output formats",
    "--all and --match filtering",
    "SIGPIPE reset (sigpipe::reset() first line of main)",
    "Daemon detection via systemd unit ExecStart lookup",
    "mtime comparison with provfs xattr fallback"
  ],
  "non_goals": [
    "No mutation: adopt scan is read-only, does not install anything",
    "No network calls",
    "No daemon/process management (that is adopt-apply's job)",
    "No tracking of running daemon staleness (that is binstale's job)",
    "Does not build binaries from source",
    "Does not manage PATH or shell config"
  ],
  "hard_constraints": {
    "rust_edition": "2021",
    "target_kind": "cli",
    "deny_unsafe": true,
    "max_deps": null,
    "msrv": "1.85",
    "additional": {
      "sigpipe_reset": "sigpipe::reset() must be the first statement in main()",
      "no_let_chains": "no let-chains (MSRV 1.85 compatibility)"
    }
  },
  "five_whys_trace": [
    {
      "why": 1,
      "q": "Why does /build ship binaries that are never used?",
      "a": "Because build = compile + commit + push, but adoption = cargo install to PATH is a separate manual step that has no enforcer."
    },
    {
      "why": 2,
      "q": "Why does no existing tool catch this?",
      "a": "binstale operates on running process PIDs \u2014 it can't flag a binary that was never installed and therefore never runs. vigil/binstale cover stale daemons; nothing covers never-installed CLIs."
    },
    {
      "why": 3,
      "q": "Why hasn't self-review caught it?",
      "a": "Self-review has flagged it in prose on six consecutive days, but prose notes have no machine-readable verdict and no actionable fix command wired to automation."
    },
    {
      "why": 4,
      "q": "Why does that matter?",
      "a": "rollout \u2014 the tool whose job is adopting fleet binaries \u2014 is itself not installed (rollout absent from PATH for 9 days), meaning the fix for fleet staleness is itself the most obvious example of the problem."
    },
    {
      "why": 5,
      "q": "Why not just manually run cargo install now?",
      "a": "The problem is structural: there are many wintermute repos and no single command shows which need adoption. The detector must exist before the fixer can be wired into automation."
    }
  ],
  "created_at": "2026-06-13T00:00:00Z",
  "carried_forward": {
    "user_persona": true,
    "unfakeable_metric": true,
    "scope": true,
    "non_goals": true,
    "hard_constraints": true,
    "five_whys_trace": true,
    "created_at": true,
    "ambiguities_resolved": true
  },
  "ambiguities_resolved": [
    {
      "question": "Should adopt scan check live running processes for staleness?",
      "resolution": "No. Running-process staleness stays binstale's job. adopt annotates is_daemon and can note binstale's verdict as context, but does not re-derive it."
    },
    {
      "question": "What is the install root convention on this laptop?",
      "resolution": "~/.local/bin (cargo install --root ~/.local). ~/.cargo/bin is also checked but not the primary fix target."
    }
  ]
}
